# SDK iOS Contract

Validaciones asociadas a licencia:

- bundleId
- Apple Team ID opcional
- environment
- minSdkVersion

El SDK puede iniciar con target opcional y mostrar captura si `allowUserInput=true`.
